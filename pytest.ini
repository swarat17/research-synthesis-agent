[pytest]
markers =
    unit: fast, no external deps
    integration: hits real APIs (needs valid keys)
    e2e: hits live Lambda endpoint

testpaths = tests/unit

addopts = --cov=src --cov-report=term-missing

# Minimum coverage enforced in CI via --cov-fail-under=70
